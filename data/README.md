# Data

Only small deterministic fixtures and data-generation instructions belong here.

Raw corpora, downloaded datasets, generated indices, model files, and private
documents must remain outside Git.
